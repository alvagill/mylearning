# minirv 单周期 RISC-V 处理器设计报告

## 项目概述
使用 Logisim 设计实现基于 RV32I 指令集的单周期处理器 `minirv`，集成 MMIO 外设，最终在 `RGB Video` 组件上成功输出“一生一芯” Logo。

## 实现指令
- **已验证通过**：`lui`, `addi`, `add`, `sw`, `lw`, `sb`, `lbu`, `jalr`
- **已实现但未用上**：`slli`, `bne`（最终测试程序仅需8条基础指令）

## 核心架构
- **寄存器堆**：32个32位寄存器，x0硬连线为0
- **PC更新**：8选1多路复用器，通过控制信号选择下一条指令地址
- **译码单元**：按opcode和funct3字段解码，控制数据通路选通
- **ALU**：模块化设计，集成加法器、减法器、移位器
- **存储结构**：哈佛结构，指令ROM与数据RAM分离，支持字节级写入

## 关键调试问题

### 译码与ALU控制
`slli`被ALU当成`addi`执行。原因是两者opcode相同，funct3不同，控制信号未正确接入ALU内部MUX。修正`is_slli`判定逻辑后解决。

### 寄存器译码错位
`addi x6, x0, 1`的数据写入了`x12`。原因是GPR内部5位译码器连线错位，`rd=00110`被错译成`01100`。用分线器校准后修复。

### 总线冲突与VGA隔离
添加VGA外设后出现随机写入和全红线。原因是`isVGA`和`isMem`地址隔离不彻底，控制信号意外接入RAM和GPR的写使能。加入`isMem AND is_store`精确隔离后解决。

### 数据RAM容量不足
`vga.hex`需要渲染256x256像素，初始RAM地址位宽不足导致地址溢出。将Address Bit Width扩展到20后，Logo正常显示。

## 最终成果
- `RGB Video`成功输出“一生一芯”Logo
- `mem.hex`和`sum.hex`测试中，PC正确跳转至`halt`，`a0`最终值为`00000000`
- 仅需8条基础指令即可完成显存像素写入

## 核心收获
1. 硬件调试中分线器是定位问题的关键工具
2. 模块隔离是避免总线冲突的基石
3. 调试时需在“怀疑电路”和“怀疑机器码”之间保持平衡
