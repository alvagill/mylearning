# minirv 单周期 RISC-V 处理器设计报告

## 1. 项目概述
本项目使用 Logisim 数字逻辑仿真软件，设计并实现了一个基于 RV32I 指令集的单周期处理器 `minirv`。项目包含基础的指令流水线（取指、译码、执行、访存、写回），并成功集成了 MMIO（内存映射 I/O）外设，实现了在 `RGB Video` 组件上输出“一生一芯”Logo 的最终目标。

## 2. 实现的指令集
处理器核心实现了以下 RISC-V 指令：
*   **基础指令**：`lui`, `addi`, `add`, `sw`, `lw`, `sb`, `lbu`, `jalr`
*   **扩展指令（经调试验证）**：`slli` (逻辑左移), `bne` (不相等跳转)

> **特别说明**：经过最终的硬件验证，`vga.hex` 测试程序实际上**并未用到 `slli` 和 `bne` 指令**，仅依靠基础 8 条指令即可完成显存像素写入。但在架构设计中，这两条指令已经完成硬件实现。

## 3. 核心硬件架构
*   **寄存器堆 (GPR)**：32 个 32 位通用寄存器，其中 `x0` 硬连线为 0，支持 5 位寄存器地址译码。
*   **PC 更新逻辑**：使用 **8 选 1 多路复用器** 作为 PC 更新选择器。通过 `is_jalr` 和 `bne_taken` 等控制信号组合成 3 位选择码（`000`为顺序执行，`001`为 `jalr`，`010`为 `bne` 等条件跳转）。
*   **指令译码单元 (`judge` 模块)**：使用优先编码器配合 7 位比较器，按 `opcode` 和 `funct3` 字段解码指令类别，并控制数据通路的选通。
*   **ALU 设计**：采用模块化设计，内部集成加法器、减法器（用于 `bne` 比较）和移位器（用于 `slli`），通过数据选择 MUX 输出最终结果。
*   **哈佛结构存储**：指令 ROM 与数据 RAM 物理分离。数据 RAM 使用 `isVGA` 地址译码器与普通内存隔离，支持字节使能（`BE0~BE3`）实现 `sb` 指令的字节级写入。

## 4. 艰苦的硬件调试与机器码纠错之旅
在项目调试过程中，遇到了大量极具挑战性的硬件死锁，这段经历对最终的架构优化起到了关键作用：

### 4.1 指令译码与ALU控制逻辑
*   **现象**：`slli` 指令被 ALU 当成 `addi` 执行。
*   **原因**：`slli` 和 `addi` 的 `opcode` 相同（均为 `0010011`），区别在于 `funct3`（`001` 与 `000`）。初期控制信号未正确接入 ALU 内部的移位器/加法器 MUX 控制端。
*   **修复**：修正 `is_slli` 判定逻辑，确 ALU 内部 MUX 在对应指令时选通移位器通道。

### 4.2 目标寄存器译码错位
*   **现象**：执行 `addi x6, x0, 1` 后，数据写入了 `x12` 寄存器；执行 `lw x4, 0(x0)` 后，数据写入了 `x9` 寄存器。
*   **原因**：GPR 子电路内部负责接收 `rd` 字段的 5 位译码器（Decoder）连线发生位错位，导致 `rd=00110`（x6）被错误译码为 `01100`（x12）。
*   **修复**：使用 Logisim 的分线器（Splitter）悬停验证指令的 `[11:7]` 位，重新校准 GPR 内部译码器的连线。

### 4.3 源寄存器读取错位
*   **现象**：`add x3, x1, x2` 指令读取的 `rs1` 不是 `x1`，而是带有错误值的 `x2`。
*   **原因**：分线器截取 `[19:15]` 位时发生线序偏移（读取到了 `x2` 的地址）。
*   **修复**：修正 GPR 内部 `RD1` MUX 的 5 位地址线序，确保 `rs1` 字段能精准选通目标寄存器。

### 4.4 总线冲突与信号隔离 (VGA外设)
*   **现象**：添加 VGA 外设后，执行 `sw` 和 `sb` 时出现了随机写入普通 RAM 或全红线的问题。
*   **原因**：`isVGA` 和 `isMem` 的地址隔离逻辑不彻底，导致 `is_bne` 等控制信号意外接入了 RAM 的写使能（`WE`）和 GPR 的写使能（`EN`）。
*   **修复**：在控制 RAM `WE` 的逻辑中加入 `isMem AND is_store` 的精确隔离，并加入 MUX 确保 `bne` 期间 GPR 的 `EN` 强制为 0。

## 5. 最终通关的“真相” (vga.hex 运行成功)
在经历了长达两天的机器码纠错后，最终发现：

1.  **`vga.hex` 并不需要 `bne` 和 `slli` 指令**：该图形程序的核心只是利用 `lui`, `addi`, `sw`, `lw` 等基础指令逐行向显存搬运像素数据。之前浪费大量时间排查这两个模块的机器码，属于无效调试。
2.  **数据 RAM 的地址位宽不足**：`vga.hex` 需要渲染 256x256 的像素（25.6万个32位字），而最初实验配置的 RAM 只有 14 位甚至更小的容量。当程序往 `0x20000000` 及以上显存区域写入数据时，因地址溢出导致数据丢失或回绕，最终屏幕只能显示空数据或白色像素。

**最终修复方案**：
*   将数据 RAM 的 **`Address Bit Width` 扩展到 `20`**。
*   保留最初已验证的 8 条基础指令配置，重新加载 `vga.hex`。

**最终成果**：
*   `RGB Video` 组件成功输出**“一生一芯”的 Logo**。
*   `mem.hex` 和 `sum.hex` 测试中，PC 正确跳转至 `halt`，且 `a0` 寄存器最终值为 **`00000000`**。

## 6. 总结与收获
本项目不仅完成了 minirv 处理器的设计，更在调试过程中收获了大量底层硬件设计的实战经验：
1.  **硬件设计比软件设计更依赖“准确无误”**：一条机器码的位错位，可能导致整整两天的无效调试。
2.  **分线器是硬件调试的“火眼金睛”**：通过 Logisim 的分线器（Splitter）直接提取并观察指令的各字段（特别是 `rd` 位），是精准定位 GPR 译码器错位和机器码错误的最有效方法。
3.  **模块隔离的重要性**：VGA 外设与普通 RAM 的写使能隔离、GPR 写使能的强制管理，是避免总线冲突和意外写入的基石。
4.  **最终结论**：在复杂的硬件验证中，**“怀疑机器码（测试代码）”** 和 **“怀疑硬件电路”** 需要保持极度谨慎的平衡。而本次项目中，绝大部分由于电路错位导致的诡异现象，最终都被证明是机器码/指令译码错误引起的。最终仅需将 RAM 位宽扩至 20 位，即可让看似复杂的逻辑彻底通畅。
